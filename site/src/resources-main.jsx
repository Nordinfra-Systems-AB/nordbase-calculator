import React from "react";
import ReactDOM from "react-dom/client";
import ResourcesApp from "./ResourcesApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ResourcesApp />
  </React.StrictMode>
);
